{
  "name": "yaps",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "npm run build && firebase emulators:start",
    "dev-watch": "npm run watch && firebase emulators:start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^10.13.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
